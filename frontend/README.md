# Pantry frontend

Vite + React app for the VINHACK recipe prototype.

```bash
cd frontend
npm install
npm run dev
```

Opens at http://localhost:5173

Demo JSON is used by default (`VITE_USE_MOCK=true`). Backend currently only exposes `GET /health`; proposed recipe search/detail/contest routes are not live yet.
